<?php

namespace Menu;

use Zend\Mvc\ModuleRouteListener;
use Zend\Mvc\MvcEvent;

class Module {

    public function onBootstrap(MvcEvent $e) {
        $eventManager = $e->getApplication()->getEventManager();
        $moduleRouteListener = new ModuleRouteListener();
        $moduleRouteListener->attach($eventManager);
        $serviceManager = $e->getApplication()->getServiceManager();

        $MenuModel = new Model\MenuModel();
        $MenuModel->initialize($serviceManager);

        //$MenuModel = new Model\MenuModel($serviceManager);
        //$MenuModel->setSm($serviceManager);

        Model\MenuModel::addMenu('Navigation');
        Model\MenuModel::addMenu('Admin');
    }

    public function getConfig() {
        return include __DIR__ . '/config/module.config.php';
    }

    public function getAutoloaderConfig() {
        return array(
            'Zend\Loader\StandardAutoloader' => array(
                'namespaces' => array(
                    __NAMESPACE__ => __DIR__ . '/src/' . __NAMESPACE__,
                ),
            ),
        );
    }

}
